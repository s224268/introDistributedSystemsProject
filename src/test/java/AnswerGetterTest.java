import org.example.AnswerGetter;

import org.example.Repository;
import org.example.Services.API;
import org.example.Services.Params;
import org.example.Services.WordDefinition;
import org.example.UserAnswerWithTimestamp;
import org.jspace.ActualField;
import org.jspace.FormalField;
import org.jspace.Space;

import java.io.IOException;
import java.util.*;

import static java.lang.Thread.sleep;

public class AnswerGetterTest {

    public static void main(String[] args) throws InterruptedException, IOException {
        /*

        Repository r = new Repository();
        Space playerSpace = r.getPlayerSpace();
        Space questionSpace = r.getQuestionSpace();
        Space gameStateSpace = r.getGameStateSpace();
        Space answerSpace = r.getAnswerSpace();
        AnswerGetter answerGetter = new AnswerGetter(answerSpace);

        answerSpace.put("answer1", "answer2", 200L);
        answerSpace.put("answer3", "answer4", 300L);
        answerSpace.put("answer5", "answer6", 400L);
        answerSpace.put("answer7", "answer8", 500L);
        answerSpace.put("answer7", "answer8", 500L);
        List<UserAnswerWithTimestamp> answersWithTimestamps = answerGetter.getAnswers(5,8);
        System.out.println(answersWithTimestamps);

         */


    }




}
